<div class="form-box">
  <% if user_signed_in? %>
    <% if current_user.id == @horse.user_id%>

      <h2>Edit details for <strong><%= @horse.name %></strong>:</h2>

      <%= render "form", horse: @horse %>
    <% end %>
  <% else %>
    <%= @horse.name %>
    <p>You'er not athoursized to be here.</p>
  <% end %>
</div>
